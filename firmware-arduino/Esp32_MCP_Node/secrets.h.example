#pragma once
// ===========================================================================
// AEDI - IONITY GLOBAL | ESP32-MCP Node Secrets (TEMPLATE)
// Copy to secrets.h in this same folder and fill in. secrets.h is git-ignored.
// (c) 2018-2026 Antwerp Designs | Ionity (Pty) Ltd
// ===========================================================================

// --- WiFi (must be the same LAN as the fleet server) -----------------------
#define WIFI_SSID           "YOUR_SSID"
#define WIFI_PASSWORD       "YOUR_WIFI_PASSWORD"

// --- MQTT broker credentials (empty strings = anonymous / dev broker) ------
#define MQTT_USERNAME       ""
#define MQTT_PASSWORD       ""

// --- Fleet enrolment token (sent as X-Fleet-Token on HTTP ingest) ----------
#define FLEET_TOKEN         "dev-fleet-token-change-me"

// --- OTA password ----------------------------------------------------------
#define OTA_PASSWORD        "CHANGE_ME_OTA"
